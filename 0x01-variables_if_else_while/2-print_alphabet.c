#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase
 * Retunn: Always 0 (seccess)
 */
int main(void)
{
	char b;

	for (b = 'a'; b <= 'z' ; b++)
	putchar(b);
	putchar('\n');
	return (0);
}
